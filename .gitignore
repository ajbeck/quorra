# macOS
.DS_Store

# Xcode per-user state
xcuserdata/
*.xcuserstate

# Swift Package Manager
.build/
.swiftpm/
Package.resolved
# …but the workspace-pinned lockfile travels with the repo so every clone
# resolves to the same aws-sdk-swift / smithy-swift / aws-crt-swift versions.
!Quorra.xcworkspace/xcshareddata/swiftpm/Package.resolved

# Claude Code / agent tooling
.agents/
.pi/
.clawpatch/
.claude/settings.local.json
.claude/skills/
/AGENTS.md
/CLAUDE.md
/.codex/
/skills-lock.json

# One-off local compiler probe, never a project artifact
/Packages/QuorraCore/check_servicekey

# Research checkouts (not part of the project)
.research/

# Build output
build/
DerivedData/
