/// Internal namespace for wire types that mirror AWS JSON request/response shapes exactly.
///
/// These types are not part of the public API — they exist solely to map between HTTP and the public data shapes
/// (`StoredOIDCClient`, `StoredSSOToken`, `DeviceVerification`).
internal enum Wire {}
