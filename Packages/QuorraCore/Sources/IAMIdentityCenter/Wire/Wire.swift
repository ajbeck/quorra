/// Internal namespace for wire types that mirror AWS JSON request/response shapes exactly.
///
/// These types are not part of the public API. OIDC wire shapes are owned by the AWS SDK;
/// the remaining wire types cover the URLSession-backed Portal client.
internal enum Wire {}
